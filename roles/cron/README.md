# cron

Manages cron jobs.

## Role Variables

| Variable | Default | Description |
|----------|---------|-------------|
| `cron_enable` | `false` | Enable cron job management |
| `cron_tab` | `[]` | List of cron jobs to configure |
