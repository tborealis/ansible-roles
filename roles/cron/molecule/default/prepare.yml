---
# The role does not create the users that own the crontab entries (the base
# role does on real hosts).
- name: Prepare
  hosts: all
  become: true
  tasks:
    - name: Create deploy user
      ansible.builtin.user:
        name: deploy
        shell: /bin/bash
