# environment

Manages environment files and dotenv configurations.

## Role Variables

| Variable | Default | Description |
|----------|---------|-------------|
| `environment_files` | `[]` | List of environment files to create |
| `dotenv_files` | `[]` | List of dotenv files to create |
