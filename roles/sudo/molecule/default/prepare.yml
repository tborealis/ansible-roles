---
# The role does not create the users it grants rules to — the base role does
# on real hosts.
- name: Prepare
  hosts: all
  become: true
  tasks:
    - name: Create deploy user
      ansible.builtin.user:
        name: deploy
        shell: /bin/bash
