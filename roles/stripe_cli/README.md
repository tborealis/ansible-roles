# stripe_cli

Installs the Stripe CLI.

## Role Variables

| Variable | Default | Description |
|----------|---------|-------------|
| `stripe_cli_version` | Required | Version of Stripe CLI to install |
